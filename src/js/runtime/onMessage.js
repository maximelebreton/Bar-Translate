/*import contextMenus from '../contextMenus'


export default chrome.runtime.onMessage.addListener(function(msg, sender, sendResponse) {
  console.log(msg, 'msg')
  if (msg.request === 'updateContextMenu') {
    contextMenus.update.translateSelection(msg.selection)
  }

})
*/
